# Consolidated Python dependencies for StudyNest (single Python package: apps/api).
# Source of truth: apps/api/pyproject.toml — keep these lists aligned when deps change.
#
# Typical usage from repo root (venv activated):
#   pip install --upgrade pip setuptools wheel
#   pip install -r requirements.txt
#   pip install -e apps/api
#
# Equivalent one-liner via extras:
#   pip install -e "apps/api[local-ai,cloud-ai,dev]"

# --- Core: FastAPI cloud API + local sidecar runtime ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7
pydantic-settings>=2.4
httpx>=0.27
supabase>=2.7.0
python-multipart>=0.0.9
ulid-py>=1.1.0

# --- Extra: local AI sidecar (llama-cpp-python / GGUF inference) ---
llama-cpp-python>=0.3.1

# --- Extra: cloud AI fallback (Gemini / OpenAI) ---
google-generativeai>=0.7.2
openai>=1.40.0

# --- Extra: development tooling ---
ruff>=0.6.0
mypy>=1.10
pytest>=8.0
pytest-asyncio>=0.23

# --- PEP 517 build backend for `pip install -e apps/api` ---
setuptools>=61
wheel
