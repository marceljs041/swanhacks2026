{
  "name": "@studynest/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "sidecar": "python -m local_sidecar.main",
    "build": "echo 'python — nothing to build'",
    "lint": "ruff check ."
  },
  "devDependencies": {
    "@types/node": "^22.5.0"
  }
}
