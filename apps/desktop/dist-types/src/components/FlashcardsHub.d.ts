/**
 * Flashcards hub. Owns deck listing, daily review banner, action chips,
 * deck grid, Review Modes + Needs Attention. Embeds the shared
 * `DeckDetailRail` to the right of the center column. The global
 * widgets-based `RightPanel` remains visible (App.tsx still mounts it
 * for view kind === "flashcards"); this screen lives entirely inside
 * `<main>` so we don't disturb it.
 */
import type { FC } from "react";
export declare const FlashcardsHub: FC;
//# sourceMappingURL=FlashcardsHub.d.ts.map