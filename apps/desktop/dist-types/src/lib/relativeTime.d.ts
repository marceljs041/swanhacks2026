/** Returns the more recent of two ISO timestamps (either may be null). */
export declare function maxIso(a: string | null, b: string | null): string | null;
/** Short relative phrase for a past ISO timestamp (matches note editor sync line). */
export declare function describeAgo(iso: string): string;
//# sourceMappingURL=relativeTime.d.ts.map