/** Static assets from `apps/desktop/public/` (copied next to `index.html` in dist). */
export declare const BRAND_LOGO_URL: string;
export declare const BRAND_HERO_URL: string;
/** Feature-specific hero illustrations (same layout sizing as `BRAND_HERO_URL`). */
export declare const BRAND_QUIZ_HERO_URL: string;
export declare const BRAND_FLASHCARD_HERO_URL: string;
export declare const BRAND_CLASS_HERO_URL: string;
export declare const BRAND_ASKAI_HERO_URL: string;
/**
 * Notes screen "Needs Attention" mascot. The asset is delivered later;
 * components that reference it should swallow `onError` so a missing
 * file doesn't break layout in the meantime.
 */
export declare const BRAND_ATTENTION_URL: string;
/** AI / Ask-AI mascot (note editor panel, etc.). */
export declare const BRAND_AI_URL: string;
//# sourceMappingURL=brand.d.ts.map